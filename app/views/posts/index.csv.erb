  <%= CSV.generate_line(['title' , 'descripton' , 'status' , 'create_user_id' , 'updated_user_id'])%>
<% @excel_admin_posts.each do |post| %>
  <% status = post.status == 1 ? 'on' : 'off' %>
  <%= CSV.generate_line([post.title , post.description , status , post.create_user.name , post.updated_user.name])%>
<% end %>
